IconButton — square, icon-only button for toolbars and compact actions; always give a `label`.
```jsx
<IconButton label="Copy" variant="secondary"><CopyIcon/></IconButton>
```
Same variants/sizes as Button; defaults to ghost.