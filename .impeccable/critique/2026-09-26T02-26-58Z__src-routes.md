---
target: the whole Web UI
total_score: 25
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 3
target_identity: "file:/home/wsladmin/dev/swagger.bot/src/routes"
timestamp: 2026-09-26T02-26-58Z
slug: src-routes
---
Method: dual-agent (A: design review subagent · B: detector + browser subagent). Target: the whole Web UI (`src/routes`, `src/components`), local production build of main `9953487` on a copy of the production Index, 390/1280, light/dark.

## Design Health Score
| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | Visibility of System Status | 3 | Rate-limited, name-required and not-in-index pages answer HTTP 200 |
| 2 | Match System / Real World | 3 | Glossary terms unexplained in place; "Develop" leads with metaphor; title/h1 mismatch on the miss view |
| 3 | User Control and Freedom | 2 | No search field on result pages; Spec viewer has no way back to its Lookup or Vendor |
| 4 | Consistency and Standards | 2 | Links underlined in some places, not others; three print variants; h1 face varies by page |
| 5 | Error Prevention | 2 | No suggestions over a 21-API Index; `strpe` goes straight to "needs a key" |
| 6 | Recognition Rather Than Recall | 2 | Exact names must be recalled; replay prints and "Last verified" aren't links |
| 7 | Flexibility and Efficiency | 2 | No autofocus or "/" shortcut; no one-click copy of a download URL |
| 8 | Aesthetic and Minimalist Design | 3 | Resolved repeats the print's facts; six stations on every result |
| 9 | Error Recovery | 3 | Miss view is exemplary; Unknown/name-required offer only "Look up another API" |
| 10 | Help and Documentation | 3 | Thorough docs, but no contextual help; MCP after eight HTTP examples |
| **Total** | | **25/40** | **Acceptable** |

## Design Specificity Verdict
Authored on Search and the Lookup result (amber bay, grease-pencil Outcome word, certainty strip, stations, the Spec as a print). Thinner on Vendors, Vendor and Docs (generic grid/list/table in the palette). Dark theme flattens the test-strip idea. Detector: CLI 1 advisory finding (`certainty-strip.tsx:54`, 0.7rem off the type ramp: real). Browser overlay on 5 pages: real = docs line length ~91 chars (`docs-view.tsx:138`), the 0.7rem size; partly real = border-width press transition (`index.tsx:115`, `docs-view.tsx:348`); on-system = lamp glow, caps figcaption, eyebrow above the Spec viewer h1; false positive = cramped-padding on the strip, text-occlusion by a closed <details>.

## Priority Issues
1. [P1] Links look exactly like text: `app.css:158` never sets `text-decoration-line`; the Spec viewer's download links, Alternates, footer and status-band links are plain text (WCAG 1.4.1). Fix: underline `a` in base; opt out nav and button-links. /impeccable harden
2. [P1] Keyless visitors dead-end with no guide to what the Index holds: no suggestions, no near-match on a miss, no "N APIs answer without a key". /impeccable onboard
3. [P1] The Resolved page buries the takeaway: print has no actions; the Current Spec list repeats it; viewer link and downloads ~1,400px down at 390. /impeccable distill, then layout
4. [P2] Dark theme breaks the certainty scale: Resolved #0e0e0e vs empty Unknown on #140b03 ~1.1:1; the Resolved patch is only an outline. /impeccable polish
5. [P2] Objects don't link to each other: the Spec viewer shows no Sources (PRODUCT.md principle) and no link to its Vendor or Lookup; replay prints and "Last verified" aren't links; name-required has no input. /impeccable harden

## Persona Red Flags
Alex (power user): 5 Tabs before the search field; no "/"; no search on result pages; no copyable download URL; `&apiVersion=` on every URL.
Sam (accessibility): download links not underlined; Resolved patch vanishes in dark; health lamp announces twice per page load; the 7-segment "1" reads as a bar.
Riley (stress tester): `strpe` → Discovery; `/lookup?name=` has no field; error pages are 200.
Priya (platform lead, PRODUCT.md): the status band has no MCP one-liner; MCP comes last in Docs; "request a key" has no stated turnaround.
Agent-linked dev (PRODUCT.md): lands on `/specs/…` with no Sources, no way to the Lookup or Vendor, download links that don't look like links; nested 80dvh scroll on a phone.

## Minor Observations
Vendor name shown 2–3 times (name == domain for all 21); the form switch uses a strip tone as UI colour (One Ladder); Lookup print always densest; dark ink and ink-2 both amber; Docs JSON wraps and loses indentation at 390; light Scalar frame is plain white; the rail "stops at 800px" is a full-page-capture artefact, not a live bug (sticky `h-dvh`).

## Questions to Consider
Should the keyless front door lead with the Index itself? Should the print be the only place a Spec's facts live, and carry the actions? Would "Develop" pass a five-second test? Six stations every time, or only the first? What would a dark theme look like where the strip is enamel in both lights?
